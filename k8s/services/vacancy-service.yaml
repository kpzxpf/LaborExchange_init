apiVersion: apps/v1
kind: Deployment
metadata:
  name: vacancy-service
  namespace: laborexchange
spec:
  replicas: 2
  selector:
    matchLabels:
      app: vacancy-service
  template:
    metadata:
      labels:
        app: vacancy-service
    spec:
      containers:
        - name: vacancy-service
          image: laborexchange/vacancy-service:latest
          imagePullPolicy: IfNotPresent
          ports:
            - containerPort: 8083
          env:
            - name: SPRING_DATASOURCE_URL
              value: jdbc:postgresql://postgres-vacancy:5432/vacancydb
            - name: SPRING_DATASOURCE_USERNAME
              valueFrom:
                secretKeyRef:
                  name: postgres-secrets
                  key: vacancy-username
            - name: SPRING_DATASOURCE_PASSWORD
              valueFrom:
                secretKeyRef:
                  name: postgres-secrets
                  key: vacancy-password
            - name: REDIS_HOST
              value: redis
            - name: REDIS_PORT
              value: "6379"
            - name: SPRING_KAFKA_BOOTSTRAP_SERVERS
              value: kafka:9092
          resources:
            requests:
              memory: "256Mi"
              cpu: "100m"
            limits:
              memory: "512Mi"
              cpu: "500m"
          readinessProbe:
            httpGet:
              path: /actuator/health
              port: 8083
            initialDelaySeconds: 30
            periodSeconds: 10
          livenessProbe:
            httpGet:
              path: /actuator/health
              port: 8083
            initialDelaySeconds: 60
            periodSeconds: 30
---
apiVersion: v1
kind: Service
metadata:
  name: vacancy-service
  namespace: laborexchange
spec:
  selector:
    app: vacancy-service
  ports:
    - port: 8083
      targetPort: 8083
